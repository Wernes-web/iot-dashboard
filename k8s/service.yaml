apiVersion: v1
kind: Service

metadata:
  name: iot-dashboard
  namespace: iot

spec:
  type: NodePort

  selector:
    app: iot-dashboard

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000